<%= image_tag @user.image.url(:medium)%>
<%= form_for @user do |f| %>
  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit 'Upload!', {method:'update', id: current_user.id}%>
  </div>
<% end %>
